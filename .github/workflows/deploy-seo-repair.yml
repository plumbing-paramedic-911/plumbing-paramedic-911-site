name: Plumbing Paramedic 911 Deployment Hold

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  hold:
    name: Production promotion in progress
    runs-on: ubuntu-latest
    steps:
      - run: echo "Automatic Namecheap deployment is temporarily held while the validated SEO/AEO release is promoted to main."
